# Nexus Node Installer 🚀

Профессиональный автоматический установщик ноды Nexus для Linux серверов. Красивый интерфейс, умная обработка ошибок и полностью автоматизированная настройка.

## Быстрая установка ⚡

```bash
curl -sSL https://raw.githubusercontent.com/titbm/nexus-node-installer/main/nexus-install.sh | bash
```

## Особенности скрипта ✨

### 🎨 **Красивый интерфейс**
- Цветные заголовки разделов (зеленый)
- Выделение важной информации цветом
- Четкая структура и прогресс установки
- Профессиональный внешний вид

### 🛡️ **Умная обработка ошибок**
- Проверка всех зависимостей (curl, tmux)
- Graceful обработка ошибок swap-файла
- Валидация Nexus ID с повторными попытками
- Информативные сообщения об ошибках

### ⚙️ **Полная автоматизация**
- Автоматическое определение пакетного менеджера (apt/yum)
- Проверка существующих установок
- Интерактивный выбор переустановки
- Завершение конфликтующих сессий

## Что делает скрипт 🔧

- ✅ **Настраивает swap файл** (настраиваемый размер, по умолчанию 12GB)
- ✅ **Проверяет и устанавливает зависимости** (curl, tmux)
- ✅ **Управляет tmux сессиями** (завершает существующие, создает новые)
- ✅ **Проверяет существующий Nexus CLI** с возможностью переустановки
- ✅ **Проверяет совместимость ОС** (Ubuntu 24.04+) и останавливает при несовместимости
- ✅ **Валидирует Nexus ID** с повторными попытками ввода
- ✅ **Запускает ноду в фоновом режиме** в защищенной tmux сессии
- ✅ **Предоставляет полные инструкции** по управлению нодой

## Системные требования 📋

- **ОС**: Ubuntu 24.04+ (обязательно), Debian или CentOS/RHEL (экспериментально)
- **Права**: sudo доступ
- **Сеть**: доступ в интернет
- **RAM**: минимум 4GB (рекомендуется 8GB+)

⚠️ **ВАЖНО**: Nexus CLI официально работает только на Ubuntu 24.04 и выше. Скрипт автоматически проверит совместимость вашей системы и остановит установку, если версия Ubuntu ниже 24.04.

## Пошаговый процесс установки 📝

### 1. **Запуск скрипта**
Выполните команду установки на вашем Linux сервере

### 2. **Настройка swap-файла** 
- Скрипт предложит выбрать размер (по умолчанию 12GB)
- Автоматически удалит существующие swap-файлы
- Создаст и активирует новый swap-файл

### 3. **Установка зависимостей**
- Проверит наличие curl и tmux
- Автоматически установит недостающие пакеты
- Поддерживает Ubuntu/Debian (apt) и CentOS/RHEL (yum)

### 4. **Управление Nexus CLI**
- Проверит существующую установку и покажет версию
- Предложит переустановку при необходимости
- Скачает и установит официальную версию CLI

### 5. **Проверка совместимости системы**
- Определит версию Ubuntu и версию Nexus CLI
- Проверит совместимость (требуется Ubuntu 24.04+)
- Остановит установку с подробным сообщением при несовместимости
- Предоставит инструкции по решению проблемы

### 6. **Получение и ввод Nexus ID**
Скрипт покажет подробные инструкции:
- Перейдите на https://app.nexus.xyz/nodes
- Войдите в аккаунт (кнопка Sign In)
- Нажмите "Add CLI Node"
- Скопируйте появившиеся цифры
- Введите ID в скрипт (до 3 попыток с валидацией)

### 7. **Запуск ноды**
- Создаст защищенную tmux сессию "nexus"
- Запустит ноду с вашим ID
- Проверит успешность запуска
- Покажет цветные сообщения о статусе

## Управление нодой 🎮

### Посмотреть логи работы
```bash
tmux attach -t nexus
```

### Выйти из логов (БЕЗ остановки ноды)
Нажмите `Ctrl+B`, отпустите, затем нажмите `D`

### Показать все сессии
```bash
tmux list-sessions
```

### Остановить ноду
```bash
tmux kill-session -t nexus
```

### Проверить статус и очки
Перейдите на https://app.nexus.xyz/nodes

## Альтернативная установка 📥

Если не хотите выполнять скрипт напрямую:

```bash
# Скачать скрипт
wget https://raw.githubusercontent.com/titbm/nexus-node-installer/main/nexus-install.sh

# Сделать исполняемым
chmod +x nexus-install.sh

# Запустить
./nexus-install.sh
```

## Возможные проблемы и решения 🛠️

### Несовместимая версия Ubuntu
Если скрипт показывает ошибку совместимости ОС:
```
❌ КРИТИЧЕСКАЯ ОШИБКА СОВМЕСТИМОСТИ
Nexus CLI работает только на Ubuntu 24.04 и выше
```

**Решения:**
- **Обновление Ubuntu**: Обновите до версии 24.04 LTS или выше
- **Новый сервер**: Используйте VPS с Ubuntu 24.04 LTS 

**Рекомендуемые версии:**
- Ubuntu 24.04 LTS (стабильная, рекомендуемая)
- Ubuntu 24.10 или новее

### Проблемы с зависимостями
```bash
# Если curl не найден
sudo apt update && sudo apt install curl -y    # Ubuntu/Debian
sudo yum install curl -y                        # CentOS/RHEL

# Если tmux не устанавливается
sudo apt update && sudo apt install tmux -y     # Ubuntu/Debian
sudo yum install tmux -y                         # CentOS/RHEL
```

### Проблемы со swap-файлом
Скрипт автоматически обрабатывает большинство проблем со swap:
- Принудительно отключает активные swap-файлы
- Завершает процессы, использующие swap
- Продолжает установку даже при ошибках swap

### Ошибки Nexus ID
- Скрипт проверяет, что ID не пустой
- Дает 3 попытки для ввода корректного ID
- Обрезает лишние пробелы автоматически

### Проблемы с tmux сессией
```bash
# Посмотреть все активные сессии
tmux list-sessions

# Принудительно завершить зависшую сессию
tmux kill-session -t nexus

# Создать новую сессию вручную
tmux new-session -d -s nexus "$HOME/.nexus/bin/nexus-network start --node-id YOUR_ID"
```

### Нода не запускается или падает
```bash
# Проверить логи подробно
tmux attach -t nexus

# Проверить статус Nexus CLI
$HOME/.nexus/bin/nexus-network --version

# Переустановить Nexus CLI
rm -rf $HOME/.nexus
curl https://cli.nexus.xyz/ | sh
```

## Безопасность 🔒

### Проверенные источники
- Nexus CLI загружается только с официального https://cli.nexus.xyz/
- Скрипт проверяет целостность загруженных файлов
- Используются только проверенные команды системы

### Безопасные права доступа
- Swap-файл создается с правами 600 (только владелец)
- Nexus CLI устанавливается в домашнюю директорию пользователя
- Не требует дополнительных системных разрешений

### Открытый исходный код
- Полный код доступен для аудита
- Никаких скрытых операций или бэкдоров
- Все действия логируются и видны пользователю

## Технические детали 🔧

### Цветовая схема интерфейса
- **🟢 Зеленый**: заголовки разделов, успешные операции
- **🔵 Голубой**: важная информация (Nexus ID)
- **🟡 Желтый**: предупреждения и важные уведомления
- **🔴 Красный**: ошибки и критические сообщения

### Структура установки
```
1. Очистка экрана и приветствие
2. Настройка swap-файла (интерактивно)
3. Установка tmux (автоматически)
4. Проверка существующих сессий
5. Установка/проверка Nexus CLI
6. Получение и валидация Nexus ID  
7. Запуск ноды в tmux
8. Финальные инструкции и команды
```

### Поддерживаемые системы
- **Ubuntu** 18.04, 20.04, 22.04, 24.04
- **Debian** 10, 11, 12
- **CentOS** 7, 8, 9
- **RHEL** 7, 8, 9
- **Rocky Linux** 8, 9
- **AlmaLinux** 8, 9

## Поддержка 💬

### Если возникли проблемы:

1. **Проверьте системные требования** - убедитесь, что у вас поддерживаемая ОС
2. **Проверьте интернет-соединение** - нужен доступ к cli.nexus.xyz 
3. **Убедитесь в наличии sudo прав** - требуется для установки пакетов
4. **Проверьте свободное место** - нужно место для swap-файла
5. **Создайте Issue** в репозитории с подробным описанием проблемы

### Полезные команды для диагностики:
```bash
# Проверить систему
cat /etc/os-release
free -h
df -h

# Проверить сеть
curl -I https://cli.nexus.xyz/

# Проверить tmux
tmux list-sessions
tmux capture-pane -t nexus -p
```

## Обновления и версии 📋

Скрипт автоматически загружает последнюю версию Nexus CLI. Для обновления ноды:

1. Остановите текущую ноду: `tmux kill-session -t nexus`
2. Запустите скрипт заново с опцией переустановки CLI
3. Используйте тот же Nexus ID

## FAQ ❓

**Q: Можно ли запустить несколько нод на одном сервере?**  
A: Нет, одна нода на сервер. Используйте разные серверы для дополнительных нод.

**Q: Что делать, если потерял Nexus ID?**  
A: Зайдите на https://app.nexus.xyz/nodes - там будут показаны все ваши ноды и их ID.

**Q: Можно ли изменить размер swap-файла после установки?**  
A: Да, запустите скрипт заново и выберите новый размер.

**Q: Как проверить, что нода работает?**  
A: Используйте `tmux attach -t nexus` для просмотра логов или проверьте статус на сайте.

**Q: Безопасно ли запускать скрипт из интернета?**  
A: Да, весь код открыт для проверки. Вы можете сначала скачать и изучить скрипт.

## История изменений 📝

### Последние улучшения:
- ✨ Добавлен красивый цветной интерфейс
- 🛡️ Улучшена обработка ошибок и валидация
- 🔧 Добавлена проверка зависимостей (curl)
- 📱 Улучшены сообщения и инструкции пользователю
- 🎯 Оптимизирован код и убрана избыточность
- 🔄 Добавлена проверка существующих установок Nexus CLI
- 🖥️ Добавлена проверка совместимости ОС (Ubuntu 24.04+)
- ⚠️ Автоматическая остановка при несовместимой версии Ubuntu

## Лицензия 📄

MIT License - используйте свободно!

---

⭐ **Не забудьте поставить звезду репозиторию, если скрипт помог!**
